#version 430

layout (local_size_x = 2, local_size_y = 3, local_size_z = 7) in;
layout (local_size_x_id = 0, local_size_y_id = 1, local_size_z_id = 2) in;

layout (set = 0, binding = 0) buffer datOut0 {
  uint data[];
} outData;

uvec3 copy;

void main() {
   copy = gl_WorkGroupSize;
   outData.data[0] = copy.x;
   outData.data[1] = copy.y;
   outData.data[2] = copy.z;
}
